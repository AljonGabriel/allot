import { Button } from 'react-bootstrap';

const AcceptRequestBtn = () => {
  return (
    <Button
      variant='primary'
      size='sm'>
      Confirm
    </Button>
  );
};

export default AcceptRequestBtn;
